"""MCP server — exposes doc-rag retrieval as a Claude Desktop tool.

Full implementation lands in P5. Scaffold exposes the shape only.
"""
from __future__ import annotations

# Note: kept as stub so `python -m doc_rag.mcp.server` is importable
# before the mcp SDK is added to dev dependencies.


def main() -> None:
    """Boot the MCP stdio server. See docs/MCP.md for registration."""
    print("doc-rag MCP server — implementation in P5 (see docs/PLAN.md)")


if __name__ == "__main__":
    main()
